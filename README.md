🚀 Drone API Gatewayโปรเจกต์นี้คือ Backend API Gateway ที่สร้างด้วย Node.js และ Express 
ทำหน้าที่เป็นตัวกลางในการจัดการข้อมูล (Configuration, Status, Logs) และรับข้อมูลบันทึกอุณหภูมิ
สำหรับฝูงโดรน โดยเชื่อมต่อกับ External Drone Service

🛠️ Requirements
Node.js(v18+)
npm

⚙️ Setup and Run
1. Installation
ติดตั้ง Dependencies:
npm install express cors dotenv
2. Configuration (.env file)
สร้างไฟล์ชื่อ .env ในโฟลเดอร์หลัก และกำหนดค่า API ภายนอก:
ข้อมูลโค้ดPORT=3001
DRONE_URL= # URL สำหรับดึงข้อมูลตั้งค่า/สถานะโดรน
DRONE_LOG= # URL สำหรับดึง/โพสต์ Logs
API_TOKEN= # Bearer Token สำหรับ API Logs
3. Running the Server
รันเซิร์ฟเวอร์:
node server.js
เซิร์ฟเวอร์จะรันอยู่ที่ http://localhost:3001

